SuccessResponse:
  type: object
  properties:
    success:
      type: boolean
    response:
      type: array
      items:
        $ref: "models/passenger.yaml"

ErrorResponse:
  type: object
  properties:
    success:
      type: boolean
    errorCode:
      type: integer
      format: int32
    errorBody:
      type: string
